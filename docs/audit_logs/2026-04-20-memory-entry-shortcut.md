## 2026-04-20 — Memory Entry Shortcut

- Removed the separate acknowledge-note UI flow.
- `Add note` on the trigger card now opens the existing Memory sidebar directly in add-note mode.
- The details section label `Notes` was renamed to `Original` to reduce confusion with review notes stored in Memory.
